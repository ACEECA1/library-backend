$kind: http-request
url: "{{baseUrl}}/api/admin/roles/bulk-assign"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "roleName": "CONTENT_MANAGER",
      "userIds": [2, 3]
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 6000
