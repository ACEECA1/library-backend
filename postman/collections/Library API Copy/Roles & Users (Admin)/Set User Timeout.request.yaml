$kind: http-request
url: "{{baseUrl}}/api/admin/users/2/timeout"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "minutes": 60
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 7000
