$kind: http-request
url: "{{baseUrl}}/api/metadata/series/1"
method: PUT
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "Harry Potter 2",
      "description": "Updated description"
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 7000
