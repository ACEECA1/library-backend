$kind: http-request
url: "{{baseUrl}}/api/reports"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "targetType": "BOOK",
      "targetId": 1,
      "reason": "Inappropriate content."
    }
auth:
  type: bearer
  credentials:
    token: "{{token}}"
order: 1000
